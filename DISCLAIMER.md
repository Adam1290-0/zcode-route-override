# Disclaimer

本项目（ZCode Route Override）以**学习与技术研究**为目的，通过本地补丁方式修改 ZCode 桌面客户端（`zcode.cjs` 与 `app.asar`），实现模型供应商级别的请求头预设改写与可选的本地代理隧道转发。

## 风险告知

1. **修改第三方软件**：本项目会向 ZCode 的 CLI 核心注入一行 `require` 并解包/重新打包其 `app.asar`。尽管补丁流程自带备份与还原脚本（`.robak`），操作失误仍可能导致 ZCode 无法启动或异常。
2. **服务条款**：修改客户端请求头可能违反 ZCode 或你所接入的 API 中转站/上游服务商的服务条款，存在账号被限制或封禁的风险。请在获得授权的前提下使用，遵守目标服务的使用规则。
3. **数据安全**：本项目为纯本地工具——请求头改写与隧道转发都在本机完成，配置保存在本地文件，**不包含任何遥测、不上传任何数据**。但请注意：不要把第三方密钥写入配置文件的 `customHeaders` 字段（配置文件为明文）。
4. **版本兼容**：ZCode 升级后补丁会失效，需要重新执行补丁脚本；不同版本间的内部结构可能变化，无法保证兼容。
5. **非官方**：本项目与 ZCode 官方（智谱）无任何关联，仅为社区研究性质工具。

## 使用前提

- 仅用于接入**你自己拥有并有权使用的** API 服务。
- 请勿将本工具用于伪造他人客户端身份盗用服务、绕过付费或其他违反平台规则的行为。

**使用即表示你已知悉并自行承担全部风险。**
